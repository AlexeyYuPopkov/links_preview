[package]
name = "links_preview"
version = "0.1.0"
edition = "2021"

description = "Backend-сервис для предпросмотра ссылок."

[dependencies]
actix-cors = "0.7.1"
actix-web = "4.13.0"
reqwest = { version = "0.11", features = ["blocking"] }
scraper = "0.26.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
